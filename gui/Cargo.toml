[package]
name = "vcs-gui"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock"] }
eframe = "0.30"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
